# ทุกอย่างต้องรันใน Terminal 

# STEP 1 : collect_data.py
# วิธีใช้ : รัน python collect_data.py เพื่อเก็บข้อมูลหรือท่าทางใหม่

# STEP 2 : train_model.py
# วิธีใช้ : รัน python train_model.py เพื่อเทรนข้อมูลท่ามือจาก ข้อ 1

# STEP 3 : run_detector.py
# วิธีใช้ : หลังจากเทรนเสร็จ ให้รัน python run_detector.py เพื่อเริ่มใช้งาน

# ขั้นตอนการเพิ่มท่าใหม่ๆ
# เข้าไปในไฟล์ collect_data.py เเล้วหาคำว่า GESTURES จากนั้นก็พิมชื่อท่าที่จะเพิ่มได้เลย
# หลังจากนั้น ให้รัน ไฟล์ python collect_data.py เพื่อเก็บข้อมูลท่าที่เภพิ่มเข้าไป โค้ดจะเทรนเเค่ท่าใหม่ท่าเก่าที่เคยเทรนจะข้ามไปทันที
# เสร็จเเล้วให้ทำตาม STEP 2 : train_model.py หลังจากนั้นให้ไป ไฟล์ run_detector.py ให้หาชื่อว่า PALETTE ให้เพิ่มชื่อท่าของเราเข้าไป ในส่วนนี้เป็นการเพิ่มชื่อเเละสีของท่าเราปรับตามใจชอบ
# หลังจากนั้น ทำตาม STEP 3 : run_detector.py ได้เลย เป็นอันเสร็จ

# สิ่งที่ต้องติดตั้ง
# Python (แนะนำ 3.9–3.11) และ Library ต่อไปนี้ติดตั้งผ่าน pip:
# pip install opencv-python mediapipe scikit-learn pandas numpy pillow

# แต่ละตัวใช้ทำอะไร:

# opencv-python — เปิดกล้องและแสดงผลภาพ
# mediapipe — ตรวจจับและ tracking มือ
# scikit-learn — เทรนโมเดล Random Forest และ LabelEncoder
# pandas — อ่าน/เขียนไฟล์ CSV
# numpy — คำนวณตัวเลข
# pillow — แสดงผลตัวอักษรภาษาไทยบนภาพ


# pip install matplotlib seaborn       # กราฟ
# pip install gTTS pygame              # เสียง TTS 

# สรุปสิ่งที่โปรเจกต์มีตอนนี้:

# ตรวจจับท่ามือ 9 ท่า real-time
# TTS พูดออกเสียงภาษาไทย
# Sentence Builder ต่อคำเป็นประโยค
# กราฟ Confusion Matrix + Learning Curve ใส่รายงาน

# 📦 ติดตั้งทั้งหมด
1. Python
# แนะนำเวอร์ชัน 3.9 — 3.11 เท่านั้น (mediapipe ยังไม่รองรับ 3.12+)
# ดาวน์โหลด: https://www.python.org/downloads/
2. ติดตั้ง Library ทั้งหมดในคำสั่งเดียว
# bashpip install opencv-python mediapipe scikit-learn pandas numpy pillow matplotlib seaborn gTTS pygame
3. เวอร์ชันที่แนะนำ (ถ้าติดปัญหา)
# pip install opencv-python==4.8.1.78
pip install mediapipe==0.10.9
pip install scikit-learn==1.3.2
pip install pandas==2.1.4
pip install numpy==1.26.4
pip install pillow==10.2.0
pip install matplotlib==3.8.2
pip install seaborn==0.13.2
pip install gTTS==2.5.1
pip install pygame==2.5.2
4. ลำดับการรันไฟล์

 # เก็บข้อมูล
python collect_data.py

# เทรน
python train_model.py

# ใช้งาน
python run_detector.py
 
 
 หมายเหตุสำคัญ

gTTS ต้องการอินเทอร์เน็ต ทุกครั้งที่พูด
ถ้า mediapipe ติดตั้งไม่ได้ ให้ลองใช้ Python 3.10 จะง่ายที่สุด
Windows ควรรัน Terminal แบบ Run as Administrator เพื่อป้องกัน permission error
